<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BrokerFi" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Bridge 主题: 兼容 MaterialComponents widget (MaterialButton等) 同时保持 AppCompat 外观 -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
<!--        Theme.Material3.DayNight.NoActionBar-->
    </style>

    <style name="Theme.BrokerFi" parent="Base.Theme.BrokerFi" />
</resources>